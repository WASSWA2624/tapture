part 'radii.dart';
part 'sizes.dart';
part 'space.dart';

/// The type this file is named for (FE-STR-06). The contract names are
/// [Space], [Radii] and [Sizes], each in its own part so a file still holds
/// one public class.
typedef Dimensions = Space;
